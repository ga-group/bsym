Usage: bsym-isin-scrape [ISIN]...

Scrape Bloomberg's bsym service for ISIN.
If omitted take input from stdin.

  --agent=STRING        Use agent string STRING instead of baiduspider.
  --raw                 Return raw API result.
